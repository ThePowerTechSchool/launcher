import FormFieldDropdown from '../form-field/form-field-dropdown'
import FormFieldText from '../form-field/form-field-text'

export const CONFIG_FORM_FIELD_VARIANTS = {
  dropdown: FormFieldDropdown,
  text: FormFieldText
}
